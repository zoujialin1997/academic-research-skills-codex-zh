---
name: ars-full
description: ARS 完整管线——研究 → 写作 → 评审 → 修改 → 定稿
---

触发 `academic-pipeline` 编排器（MODE_REGISTRY.md 中的 `(pipeline)`——编排器本身没有命名模式）。加载 skill 并执行完整的学术研究工作流（10 阶段编排：deep-research → academic-paper → integrity → academic-paper-reviewer → revision → re-review → final integrity → finalize）。

模式参考：`MODE_REGISTRY.md` § academic-pipeline。
Skill 入口：`academic-pipeline/WORKFLOW.md`。
