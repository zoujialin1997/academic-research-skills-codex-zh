# Cross-document consistency advisory — UNMEASURED

This directory documents the measurement boundary for #672. The repository's
invented fixtures exercise only closed-contract validation, source/UTF-8 replay,
failure states, deterministic identifiers/digests, consumer wiring, paging, and
legacy isolation.

They do **not** measure semantic accuracy, inconsistency coverage, agreement,
false-positive rate, false-negative rate, usefulness, or efficacy. A
`NO_LISTED_INCONSISTENCY_LOCATED` fixture is not proof of agreement,
completeness, or a clean document.

There are no `measurement-*.json` files, raw model outputs, judge records, or
accuracy claims here. No live model, external API, network service, human/model
judge, existing Codex multi-file audit, or expensive evaluation is required or
authorized. The final carrier therefore remains
`evaluation_status=UNMEASURED`.
