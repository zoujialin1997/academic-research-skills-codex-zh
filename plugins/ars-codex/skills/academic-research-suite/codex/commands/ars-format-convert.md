---
name: ars-format-convert
description: ARS academic-paper `format-convert` 模式——转换为 LaTeX / DOCX / PDF / Markdown
model: sonnet
---

以 `format-convert` 模式触发 `academic-paper` skill。在 LaTeX、DOCX（经 Pandoc）、PDF 或 Markdown 之间转换论文，并在主要格式之间转换引文样式。保真度谱系，低监督。

模式参考：`MODE_REGISTRY.md` § academic-paper。
Skill 入口：`academic-paper/WORKFLOW.md`。
